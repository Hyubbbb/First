# 사칙연산
print(1 + 1)
print(1 - 2)
print(2 * 2)
print(5 / 2)


print(4 / 2) # / 연산자는 나누어 떨어지는 경우도 실수형

print(5.5 // 2) # // 연산자는 몫값만 출력함에도 불구하고 실수형

print(5 % 2)

print(2 ** 10)

print(int(3.3)) # 실수형 -> 정수형 "소숫점은 버림!!"

# type() 함수를 통해서 객체의 타입(자료형)을 알아낼 수 있다.

print(divmod(5, 2)) # 5를 2로 나누었을 때의 몫, 나머지가 동시에 '튜플'형태로 출력된다.

# 2진수: 0b~
# 8진수: 0o~
# 16진수: 0x~ OR 0X~
