# 한 번에 여러 값 출력하기

print(1, 2, 3) # 각 값이 공백으로 띄워진 한 줄로 출력

# 공백 대신 sep로 값 사이에 문자 넣기
print(4, 5, 6, sep=", ")

print(7, 8, 9, sep='') # 빈 문자열을 지정해서 붙여쓸 수도 있다.

print(1920, 1080, sep="x") # 일반적인 문자(열)를 넣을 수도 있다.
